# Notes

- `TotalCharges` has blanks in the Kaggle CSV; convert to numeric with NaN handling.
- Treat `Churn` as the binary target (Yes=1, No=0).
- Use `customerID` only for reporting, not as a feature.
